import { Container, Typography } from "@mui/material";

export default function ProductsPage() {
  return (
    <Container sx={{ border: "1px solid #ff0000", minHeight: "100vh", my: 2 }}>
      <Typography variant="h1">This is the Products Page</Typography>
    </Container>
  );
}
